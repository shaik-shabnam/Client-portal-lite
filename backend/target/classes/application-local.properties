# Local override — loaded automatically when spring.profiles.active=local
# Passwords with special characters are safe here because this file
# is loaded AFTER the main application.properties
spring.datasource.password=June@4#2k5
